/**
 * Recording constants.
 *
 * Re-exports from @openheaders/core — single source of truth.
 */

export { MESSAGE_TYPES, RECORDING_STATES } from '@openheaders/core/protocol';
export type { MessageType, RecordingStateType } from '@openheaders/core/protocol';
